{{ define "content" }}
	{{ $logged := .LoggedIn }}
	{{ if $logged }}
		{{ range .Posts }}
		<div class="container">
			<form action="/edit/{{ .ID }}" method="post" obsubmit="return postContent()">
			<div class="title">
				<label>Title: </label>
				<input type="text" name="title" style="background:rgba(0,0,0,0);outline:none;" value="{{ .Title }}"/><br>
			</div><br>
			<textarea id="summernote" class="content" name="content">{{ .Content }}</textarea><br>
			<div class="post_footer">
				<input type="submit" value="Submit" name="submit"/>
			</div>
			</form>
		</div>
	{{ end }}
	{{ else }}
		<a href="/login">LogIn</a>
	{{ end }}
{{ end }}
